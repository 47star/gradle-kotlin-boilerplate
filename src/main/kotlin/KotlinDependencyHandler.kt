fun kotlinx(name: String, version: String = "latest.release") =
    "org.jetbrains.kotlinx:kotlinx-$name:$version"

fun ktor(name: String, version: String = "latest.release") =
    "io.ktor:ktor-$name:$version"
